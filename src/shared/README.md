# Shared Modules

Shared modules live here and are synced into `ReplicatedStorage.Shared`.

Use this folder for declarative definitions that both client and server need:

- `DialogueDefinitions`
- `QuestDefinitions`
- `StatusEffectDefinitions`
- `PassportDefinitions`
- `ItemDefinitions`
- `BadgeDefinitions`
- utility modules

