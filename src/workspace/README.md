# Workspace-Bound Content

Workspace-bound scripts and models live here and are synced into `Workspace`.

Use this folder for interactable-specific scripts or Rojo-managed workspace objects such as:

- `WaiverKiosk`
- `MetamorphosisBed`
- `Stapler`
- `BreadMoldCase`
- `ChinchillaHabitat`
- `LivingPaintingGallery`

